on: 
  push:

jobs:
  secret-job:
    runs-on: 
      - ubuntu-latest
    steps:
      - name: Accessing Secret
        id: step2
        run: |
          echo "Secret vaule is - $GITHUB_ACTOR"
          $github.actor