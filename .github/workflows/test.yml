on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: exit 1

  test:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
    - run: echo test

  deploy:
    needs: [test, build]
    runs-on: ubuntu-latest
    steps:
    - run: echo deploy

  reports:
    runs-on: ubuntu-latest
    steps:
    - run: echo reports

        